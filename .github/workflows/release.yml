name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            artifact_name: repo-context
            asset_name: repo-context-linux-x86_64
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            artifact_name: repo-context.exe
            asset_name: repo-context-windows-x86_64.exe
          - os: macos-latest
            target: x86_64-apple-darwin
            artifact_name: repo-context
            asset_name: repo-context-macos-x86_64
          - os: macos-latest
            target: aarch64-apple-darwin
            artifact_name: repo-context
            asset_name: repo-context-macos-arm64

    steps:
      - uses: actions/checkout@v4
      
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
      
      - name: Build
        run: cargo build --release --target ${{ matrix.target }}
      
      - name: Upload binaries to release
        uses: svenstaro/upload-release-action@v2
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: target/${{ matrix.target }}/release/${{ matrix.artifact_name }}
          asset_name: ${{ matrix.asset_name }}
          tag: ${{ github.ref }}
          overwrite: true